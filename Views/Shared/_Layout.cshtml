@using RealEstatePro.Services
@inject ITranslationService TranslationService
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@{
    var currentCulture = Context.Request.Cookies["Language"] ?? "en";
    var t = TranslationService.GetTranslations(currentCulture);
    var isRtl = currentCulture == "ar";
    var dir = isRtl ? "rtl" : "ltr";
}
<!DOCTYPE html>
<html lang="@currentCulture" dir="@dir">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Object</title>
    <link rel="stylesheet" href="~/css/style.css" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" crossorigin="" />
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=" crossorigin=""></script>
    @if (isRtl)
    {
        <style>
            body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
        </style>
    }
</head>
<body>
    <nav class="navbar">
        <div class="container nav-content">
            <a asp-controller="Home" asp-action="Index" class="nav-brand">🏢 Object</a>
            
            <div class="nav-links">
                <a asp-controller="Home" asp-action="Index" class="nav-link">@t["nav_home"]</a>
                <a asp-controller="Properties" asp-action="Index" class="nav-link">@t["nav_browse"]</a>
                
                @if (User.Identity?.IsAuthenticated == true)
                {
                    <a asp-controller="Properties" asp-action="Create" class="nav-link">@t["nav_dashboard"]</a>
                    <a asp-controller="Properties" asp-action="MyProperties" class="nav-link">@t["my_properties"]</a>
                    <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                        <button type="submit" class="btn btn-outline" style="padding: 0.35rem 0.85rem; font-size: 0.85rem;">@t["nav_logout"]</button>
                    </form>
                }
                else
                {
                    <a asp-controller="Account" asp-action="Login" class="nav-link">@t["nav_login"]</a>
                    <a asp-controller="Account" asp-action="Register" class="btn btn-primary" style="padding: 0.4rem 1rem; font-size: 0.85rem;">@t["nav_signup"]</a>
                }
                
                <div class="language-selector">
                    <select onchange="window.location.href='@Url.Action("ChangeLanguage", "Account")?culture='+this.value+'&returnUrl=@Context.Request.Path'"
                            class="form-control" style="padding: 0.25rem 0.5rem; width: auto; min-width: 110px;">
                        @{
                            var languages = new Dictionary<string, string> 
                            { 
                                {"en", "🇺🇸 English"}, 
                                {"ar", "🇸🇦 العربية"}
                            };
                        }
                        @foreach (var lang in languages)
                        {
                            @if (currentCulture == lang.Key)
                            {
                                <option value="@lang.Key" selected>@lang.Value</option>
                            }
                            else
                            {
                                <option value="@lang.Key">@lang.Value</option>
                            }
                        }
                    </select>
                </div>
            </div>
        </div>
    </nav>

    <main style="min-height: 80vh;">
        @if (TempData["SuccessMessage"] != null)
        {
            <div class="container" style="padding-top: 1rem;">
                <div class="alert alert-success">
                    @TempData["SuccessMessage"]
                </div>
            </div>
        }
        @RenderBody()
    </main>

    <footer class="footer">
        <div class="container footer-content">
            <div>
                <h3>🏢 Object</h3>
                <p class="footer-copy">@t["footer_about_desc"]</p>
                <p class="footer-copy" style="margin-top: 0.5rem;">@t["footer_rights"] &copy; @DateTime.Now.Year</p>
            </div>
            <div>
                <h4>@t["footer_links"]</h4>
                <ul>
                    <li><a asp-controller="Home" asp-action="Index">@t["nav_home"]</a></li>
                    <li><a asp-controller="Properties" asp-action="Index">@t["nav_browse"]</a></li>
                    <li><a asp-controller="Account" asp-action="Login">@t["nav_login"]</a></li>
                </ul>
            </div>
            <div>
                <h4>@t["footer_contact"]</h4>
                <ul>
                    <li>📧 info@object.sa</li>
                    <li>📞 +966 11 000 0000</li>
                    <li>📍 @(isRtl ? "الرياض، المملكة العربية السعودية" : "Riyadh, Saudi Arabia")</li>
                </ul>
            </div>
        </div>
    </footer>
    
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
